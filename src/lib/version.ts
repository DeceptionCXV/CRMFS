export const BUILD_NUMBER = 580;
export const VERSION_STRING = `v0.12.0.${BUILD_NUMBER}`;
export const FULL_VERSION_STRING = `Version 0.12.0.${BUILD_NUMBER}`;

/** Mosque paper / registration form process revision (shown on Add Member). */
export const APPLICATION_VERSION = 'V0.12.0.580';
/** Value stored on member records (`paper_form_version`). */
export const PAPER_FORM_VERSION = 'v01.25';
